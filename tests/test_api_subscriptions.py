

def test_feed_subscribe():
    """
    Tests subscribe to feed endpoint. POST /api/feeds/subscribe
    - Subscribe to a feed. Email is sent out to confirm and filter is pending
    """
    assert False

def test_feed_subscribe_confirm():
    """
    Tests feed subscribe confirm endpoint. POST /api/feeds/subscribe-confirm
    - Confirm a subscription to a feed
    """
    assert False

def test_feed_unsubscribe():
    """
    Tests feed unsubscribe endpoint. POST /api/feeds/unsubscribe
    - Unsubscribe from a feed
    """
    assert False
